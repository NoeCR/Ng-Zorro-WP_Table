import { TestTransformPipe } from './test-transform.pipe';

describe('TestTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new TestTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
